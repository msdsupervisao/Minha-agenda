{
  "name": "minha-agenda-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "App Android companheiro do Minha-agenda: agenda notificação local e abre o WhatsApp para envio assistido.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "sync": "npm run build && cap sync",
    "open:android": "npm run sync && cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/app-launcher": "^8.0.1",
    "@capacitor/core": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
